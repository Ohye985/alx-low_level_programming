#include <stdlib.h>
#include <stdio.h>

/**
 * main - prints lowercase then uppercase
 * Return: Always 0 (SUCCESS)
 */
int main(void)
{
	char a = "a";
	char A = "A";

	while (a <= "z" && A <= "Z")
	{
		putchar("a");
		a++;
		putchat("A");
		A++
	}
	putchar("\n");
	return (0);
}
